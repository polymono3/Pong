#pragma once

const int SCREEN_WIDTH = 800;
const int SCREEN_HEIGHT = 600;

const int WALL_THICKNESS = 15;
const int PADDLE_THICKNESS = 5;
const int PADDLE_HEIGHT = 40;
const int BALL_SIZE = 10;

const float PADDLE_SPEED = 500.0f;

const float INITIAL_BALL_SPEED = 500.0f;
const float MAX_BALL_SPEED = 700.0f;

const float PI = 3.14159f;

const float MAX_BOUNCE_ANGLE = 45.0f * PI / 180.0f;